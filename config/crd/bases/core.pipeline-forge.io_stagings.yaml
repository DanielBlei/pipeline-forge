---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stagings.core.pipeline-forge.io
spec:
  group: core.pipeline-forge.io
  names:
    kind: Staging
    listKind: StagingList
    plural: stagings
    singular: staging
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Staging is the Schema for the stagings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of Staging
            properties:
              description:
                description: Optional human-readable description of this staging step
                type: string
              ingest:
                description: Ingest defines how the pipeline is initiated (e.g., via
                  CronJob or Trigger)
                properties:
                  cronjob_ref:
                    description: Reference to an existing CronJob resource (used if
                      mode=reference)
                    properties:
                      kind:
                        description: Kind of the referenced object (usually "CronJob")
                        enum:
                        - CronJob
                        type: string
                      name:
                        description: Name of the referenced CronJob
                        type: string
                      namespace:
                        description: |-
                          Namespace where the referenced CronJob exists.
                          If omitted, defaults to the namespace of the Staging resource.
                        type: string
                    required:
                    - kind
                    - name
                    type: object
                  mode:
                    description: 'Mode determines how this stage is triggered: by
                      a CronJob reference or a Trigger CRD'
                    enum:
                    - reference
                    - trigger
                    type: string
                  owner:
                    description: Owner indicates who owns or maintains this ingestion
                      stage (optional metadata)
                    type: string
                  trigger:
                    description: Reference to a Trigger resource (used if mode=trigger)
                    properties:
                      name:
                        description: Name of the Trigger resource (required)
                        type: string
                      namespace:
                        description: |-
                          Namespace where the Trigger resource is defined.
                          If omitted, defaults to the namespace of the Staging resource.
                        type: string
                    required:
                    - name
                    type: object
                required:
                - mode
                type: object
              transform:
                description: Transform defines the dbt-based transformation logic
                properties:
                  full_refresh:
                    description: Whether to run dbt with --full-refresh
                    type: boolean
                  models:
                    description: List of dbt models to execute
                    items:
                      type: string
                    minItems: 1
                    type: array
                  owner:
                    description: Optional owner metadata for the transform stage
                    type: string
                  project:
                    description: Name of the dbt project this stage executes
                    type: string
                  target:
                    description: dbt target profile to use (e.g. "dev", "prod")
                    type: string
                required:
                - models
                - project
                - target
                type: object
            required:
            - ingest
            - transform
            type: object
          status:
            description: Status defines the observed state of Staging
            properties:
              status:
                type: string
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
