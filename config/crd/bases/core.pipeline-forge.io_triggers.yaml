---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: triggers.core.pipeline-forge.io
spec:
  group: core.pipeline-forge.io
  names:
    kind: Trigger
    listKind: TriggerList
    plural: triggers
    singular: trigger
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Trigger is the Schema for the triggers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Trigger
            properties:
              bigquery:
                description: BigQueryTriggerSpec defines a trigger based on BQ table
                  freshness
                properties:
                  dataset:
                    description: Dataset ID (e.g. "raw_salesforce")
                    type: string
                  freshness:
                    description: Max allowed staleness before triggering is skipped
                    type: string
                  table:
                    description: Table ID (e.g. "leads")
                    type: string
                required:
                - dataset
                - table
                type: object
              gcs:
                description: GCSTriggerSpec defines a trigger based on GCS file drops
                properties:
                  bucket:
                    description: GCS bucket name (without gs:// prefix)
                    type: string
                  freshness:
                    description: Max allowed staleness before triggering is skipped
                    type: string
                  prefix:
                    description: Optional prefix to match files (e.g. "exports/")
                    type: string
                  waitForFile:
                    description: Wait until file exists before triggering
                    type: boolean
                required:
                - bucket
                type: object
              pubsub:
                description: PubSubTriggerSpec defines a trigger from a Pub/Sub message
                properties:
                  messageFilter:
                    description: Optional message filter
                    properties:
                      attribute:
                        description: Attribute name to match (e.g. "dataset")
                        type: string
                      equals:
                        description: Required match value
                        type: string
                    required:
                    - attribute
                    - equals
                    type: object
                  topic:
                    description: Pub/Sub topic to subscribe to
                    type: string
                required:
                - topic
                type: object
              type:
                description: 'Type determines the trigger mechanism: cron, gcs, pubsub,
                  bigquery, etc.'
                enum:
                - gcs
                - pubsub
                - bigquery
                type: string
            required:
            - type
            type: object
          status:
            description: status defines the observed state of Trigger
            properties:
              status:
                type: string
            required:
            - status
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
