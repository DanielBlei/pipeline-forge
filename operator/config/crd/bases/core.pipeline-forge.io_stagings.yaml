---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: stagings.core.pipeline-forge.io
spec:
  group: core.pipeline-forge.io
  names:
    kind: Staging
    listKind: StagingList
    plural: stagings
    shortNames:
    - stg
    singular: staging
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Overall staging status
      jsonPath: .status.status
      name: Status
      priority: 1
      type: string
    - description: Owner
      jsonPath: .spec.owner
      name: Owner
      priority: 1
      type: string
    - description: Ingest type
      jsonPath: .spec.ingest.type
      name: Ingest
      type: string
    - description: Ingest status
      jsonPath: .status.ingest.status
      name: Ingest Status
      type: string
    - description: Transform status
      jsonPath: .status.transform.status
      name: Transform Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Staging is the Schema for the stagings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines the desired state of Staging
            properties:
              description:
                description: Optional human-readable description of this staging step
                type: string
              ingest:
                description: Ingest defines how the pipeline is initiated, either
                  by referencing a Kubernetes CronJob or Job or by referencing a Trigger
                  CRD
                properties:
                  args:
                    description: |-
                      Args specifies the command-line arguments to pass to the container,
                      overriding the default container command (entrypoint).
                      If set, these arguments will replace the container's default command.
                    items:
                      type: string
                    type: array
                  image:
                    description: |-
                      Image is the container image used to run the ingest. (only relevant when mode is "bootstrap")
                      This image is launched in a Kubernetes from a Job or Cronjob created by the Staging controller.
                    type: string
                  maxRetry:
                    description: |-
                      MaxRetry defines the maximum number of times to retry the transform step on failure.
                      If not set, defaults to 0 (no retries).
                    format: int32
                    type: integer
                  mode:
                    description: |-
                      Mode determines whether the ingestion source is a reference to an existing resource,
                      or should be created and managed by this Staging resource in the future.

                      Accepted values:
                      - "reference": reference an existing CronJob, Job, or Trigger
                      - "bootstrap": placeholder for future functionality to create the ingestion job
                    enum:
                    - reference
                    - bootstrap
                    type: string
                  name:
                    description: |-
                      Name of the ingestion resource.
                      This must match the Kubernetes or custom resource to be watched.
                    type: string
                  namespace:
                    description: |-
                      Namespace where the ingestion resource is defined.
                      If omitted, defaults to the namespace of the Staging object.
                    type: string
                  owner:
                    description: Owner identifies the team or user responsible for
                      this ingestion step.
                    type: string
                  pollIntervalSeconds:
                    description: |-
                      PollIntervalSeconds specifies how frequently (in seconds) the controller checks for changes
                      or status updates in the ingestion resource.
                      If not set, defaults to 120 seconds.
                    format: int32
                    type: integer
                  resources:
                    description: |-
                      Resources defines CPU and memory requests/limits for the Cronjob/Job container.
                      If Type is "trigger", this field is ignored.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  schedule:
                    description: |-
                      Schedule defines the cron schedule (in standard cron format) for the ingestion job.
                      This field is only relevant when Mode is "bootstrap" and Type is "cronjob".
                      If Mode is "reference" or Type is not "cronjob", this field is ignored.
                    type: string
                  suspend:
                    description: |-
                      Suspend indicates whether the ingestion action is currently suspended.
                      If true, the controller will not process or watch the referenced ingestion resource.
                    type: boolean
                  type:
                    description: |-
                      Type specifies the kind of resource used to signal ingestion readiness.

                      Accepted values:
                      - "cronjob": watches for successful Job completions from a CronJob
                      - "job": watches a single-use Kubernetes Job
                      - "trigger": watches a custom Trigger resource (e.g., based on PubSub, GCS, BigQuery)
                    enum:
                    - cronjob
                    - job
                    - trigger
                    type: string
                required:
                - mode
                - name
                - type
                type: object
              owner:
                description: Optional owner of the StagingSpec (distinct from the
                  owner of the ingest or transform stages)
                type: string
              transform:
                description: Transform defines the dbt-based transformation logic
                properties:
                  args:
                    description: |-
                      Args specifies the command-line arguments to pass to the container,
                      overriding the default container command (entrypoint).
                      If set, these arguments will replace the container's default command.
                    items:
                      type: string
                    type: array
                  engine:
                    default: dbt
                    description: |-
                      Engine defines the transformation engine to use (e.g., "dbt", "spark", "custom").
                      Defaults to "dbt" if not specified.
                    enum:
                    - dbt
                    - spark
                    - custom
                    type: string
                  full_refresh:
                    description: |-
                      Whether to force a full refresh of data (e.g., `dbt run --full-refresh`).
                      Only applicable to engines that support it (like dbt).
                    type: boolean
                  image:
                    description: |-
                      Image is the container image used to run the transform.
                      This image is launched in a Kubernetes Job created by the Staging controller.
                    type: string
                  maxRetry:
                    description: |-
                      MaxRetry defines the maximum number of times to retry the transform step on failure.
                      If not set, defaults to 0 (no retries).
                    format: int32
                    type: integer
                  models:
                    description: |-
                      Models defines the transformation model names to run.
                      For dbt, these are model names. For other engines, these could be script names, tasks, etc.
                    items:
                      type: string
                    type: array
                  name:
                    description: Name is a reference or descriptive label for this
                      transform step.
                    type: string
                  owner:
                    description: Owner identifies the team or individual responsible
                      for the transformation.
                    type: string
                  project:
                    description: Project is the name of the transformation project
                      (e.g., dbt project name).
                    type: string
                  resources:
                    description: Resources defines CPU and memory requests/limits
                      for the Job container.
                    properties:
                      claims:
                        description: |-
                          Claims lists the names of resources, defined in spec.resourceClaims,
                          that are used by this container.

                          This field depends on the
                          DynamicResourceAllocation feature gate.

                          This field is immutable. It can only be set for containers.
                        items:
                          description: ResourceClaim references one entry in PodSpec.ResourceClaims.
                          properties:
                            name:
                              description: |-
                                Name must match the name of one entry in pod.spec.resourceClaims of
                                the Pod where this field is used. It makes that resource available
                                inside a container.
                              type: string
                            request:
                              description: |-
                                Request is the name chosen for a request in the referenced claim.
                                If empty, everything from the claim is made available, otherwise
                                only the result of this request.
                              type: string
                          required:
                          - name
                          type: object
                        type: array
                        x-kubernetes-list-map-keys:
                        - name
                        x-kubernetes-list-type: map
                      limits:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Limits describes the maximum amount of compute resources allowed.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                      requests:
                        additionalProperties:
                          anyOf:
                          - type: integer
                          - type: string
                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                          x-kubernetes-int-or-string: true
                        description: |-
                          Requests describes the minimum amount of compute resources required.
                          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                          otherwise to an implementation-defined value. Requests cannot exceed Limits.
                          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                        type: object
                    type: object
                  suspend:
                    description: |-
                      Suspend indicates whether the Transform action is currently suspended.
                      If true, the controller will not process the transform step(s)
                    type: boolean
                  target:
                    description: Target defines the execution target or environment
                      profile (e.g., "dev", "prod").
                    type: string
                required:
                - image
                - name
                - project
                - target
                type: object
            required:
            - ingest
            - transform
            type: object
          status:
            description: Status defines the observed state of Staging
            properties:
              conditions:
                description: Conditions is a list of status conditions for the Staging
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              ingest:
                description: Ingest is the status of the ingestion step.
                properties:
                  attempts:
                    description: Attempts tracks the number of times this step has
                      been attempted.
                    format: int32
                    type: integer
                  failedAttempts:
                    description: FailedAttempts tracks the number of failed attempts.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: LastAttemptTime is the timestamp of the last attempt.
                    format: date-time
                    type: string
                  lastCheckedTime:
                    description: LastCheckedTime is the last time the status was checked.
                    format: date-time
                    type: string
                  lastCompletedTime:
                    description: LastCompletedTime is the last time the step (e.g.,
                      ingestion CronJob) completed.
                    format: date-time
                    type: string
                  lastFailureTime:
                    description: LastFailureTime is the timestamp of the last failure.
                    format: date-time
                    type: string
                  lastRunJobName:
                    description: LastRunJobName is the name of the last Kubernetes
                      Job created by this resource.
                    type: string
                  maxRetries:
                    description: MaxRetries defines the maximum number of retries
                      allowed.
                    format: int32
                    type: integer
                  message:
                    description: Message provides additional information or error
                      messages about the step status.
                    type: string
                  retryCount:
                    description: RetryCount tracks the number of retries for the current
                      attempt.
                    format: int32
                    type: integer
                  status:
                    description: Status of the associated staging object (e.g., Complete,
                      Running, Failed, Pending, Unknown).
                    type: string
                  successfulAttempts:
                    description: SuccessfulAttempts tracks the number of successful
                      attempts.
                    format: int32
                    type: integer
                type: object
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller.
                format: int64
                type: integer
              status:
                description: Status is the status of the Staging (e.g., Deployed,
                  Failed, Pending, Running).
                type: string
              transform:
                description: Transform is the status of the transformation step.
                properties:
                  attempts:
                    description: Attempts tracks the number of times this step has
                      been attempted.
                    format: int32
                    type: integer
                  failedAttempts:
                    description: FailedAttempts tracks the number of failed attempts.
                    format: int32
                    type: integer
                  lastAttemptTime:
                    description: LastAttemptTime is the timestamp of the last attempt.
                    format: date-time
                    type: string
                  lastCheckedTime:
                    description: LastCheckedTime is the last time the status was checked.
                    format: date-time
                    type: string
                  lastCompletedTime:
                    description: LastCompletedTime is the last time the step (e.g.,
                      ingestion CronJob) completed.
                    format: date-time
                    type: string
                  lastFailureTime:
                    description: LastFailureTime is the timestamp of the last failure.
                    format: date-time
                    type: string
                  lastRunJobName:
                    description: LastRunJobName is the name of the last Kubernetes
                      Job created by this resource.
                    type: string
                  maxRetries:
                    description: MaxRetries defines the maximum number of retries
                      allowed.
                    format: int32
                    type: integer
                  message:
                    description: Message provides additional information or error
                      messages about the step status.
                    type: string
                  retryCount:
                    description: RetryCount tracks the number of retries for the current
                      attempt.
                    format: int32
                    type: integer
                  status:
                    description: Status of the associated staging object (e.g., Complete,
                      Running, Failed, Pending, Unknown).
                    type: string
                  successfulAttempts:
                    description: SuccessfulAttempts tracks the number of successful
                      attempts.
                    format: int32
                    type: integer
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
